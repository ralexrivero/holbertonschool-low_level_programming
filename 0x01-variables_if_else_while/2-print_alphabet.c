#include <stdio.h>
/**
*main - print alphabet
*
 *Description: display alphabet
 *Return: 0 if the programming ran to completion
 */

int main(void)
{
char alpha;
int i;

for (i = 1 ; i <= 200 ; i++)
putchar(alpha);
putchar('\n');
return (0);
}
