#include "holberton.h"

/**
 * times_table - ciclo anidado
 * Description: print 9 times table
 * Return: 0 when done
 */

void times_table(void)
{
int i, j;

	for (i = 0; i <= 9; i++)
	{
		for (j + (i + 1);; i += 9)
		{
			_putchar (i);
			_putchar (',');
			_putchar (' ');
			_putchar (' ');
		}
	}
