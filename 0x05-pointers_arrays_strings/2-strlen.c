#include "holberton.h"
#include <stdio.h>
/**
 * _strlen - prototype function
 * Description: return length of a string
 * @s: the string
 * Return: the value
 */

int _strlen(char *s)
{
printf("%lu", sizeof(*s));
return (0);
}
