#include <stdio.h>

/**
 *_islower - check the lowercase
 *@c: the character to chek lowercase
 *Description: _putchar prototype
 *Return: - if is lowercase 1, 0 other values
 */

int _islower(int c)
{
return (c >= 97 && c <= 122);
}
